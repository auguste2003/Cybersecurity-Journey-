# Prompt the user to enter their age
age = input("Enter your age: ")

# Convert the input to an integer
age = int(age)

# Print the age to confirm it was stored correctly
print("You are " + str(age) + " years old.")
